import java.util.*;

class square
{
    public static void main(String args[])
    {
        int x=0;
        double r=Math.sqrt(x);
        System.out.println("the square of " + x + " is "+r);
        if(x==0)
        {
            System.out.println("the square of zero is 0");
        }
    }
}
